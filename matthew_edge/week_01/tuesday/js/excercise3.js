/*MixUp

Create a function called mixUp. It should take in two strings, and return the concatenation of the two strings (separated by a space) slicing out and swapping the first 2 characters of each. You can assume that the strings are at least 2 characters long. For example:

  mixUp('mix', 'pod'): 'pox mid'
  mixUp('dog', 'dinner'): 'dig donner'
Look up the JavaScript string reference to find methods which may be useful!
*/
var mixUp = function(string1, string2){
  var fir = string1.substring([0,1])
  var sec = string2.substring([0,1])
  var fff = string1.replace([0,1/sss])
  var sss = string2.replace([0,1/fir])

}


//var firstChar1 = str1.slice(0,1)


//return b.slice + a.slice
